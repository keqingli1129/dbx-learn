"""One-off bootstrap run before, or alongside, the pipelines.

Seeds the simulated operational source, downloads the labelled damage images,
emits telematics event files, and performs the insert/update/delete mutations
that prove change capture works. All data produced here is synthetic
(Constitution V).
"""
