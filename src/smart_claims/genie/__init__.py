"""Genie space creation.

Asset Bundles have no `genie` resource type, so this space is created by an
idempotent script driven by the same configuration values the bundle uses --
not by a documented click-path (Constitution IV).
"""
